-- LumosUI Library (Jednoduchší a kompatibilnejší dizajn)
-- Autor: TvůjNázev
-- Dátum: 25. 3. 2025

local LumosUI = {}

-- Funkcia na vytvorenie okna
function LumosUI:CreateWindow(title, size)
    local ScreenGui = Instance.new("ScreenGui")
    local Frame = Instance.new("Frame")
    local Title = Instance.new("TextLabel")

    ScreenGui.Name = "LumosUI_ScreenGui"
    ScreenGui.ResetOnSpawn = false -- Aby sa UI nezmizlo
    ScreenGui.Parent = game:GetService("CoreGui")

    Frame.Name = "MainFrame"
    Frame.Parent = ScreenGui
    Frame.Size = size or UDim2.new(0, 400, 0, 300)
    Frame.Position = UDim2.new(0.5, -200, 0.5, -150)
    Frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Frame.BorderSizePixel = 0
    Frame.AnchorPoint = Vector2.new(0.5, 0.5)

    Title.Name = "TitleLabel"
    Title.Parent = Frame
    Title.Text = title or "LumosUI Window"
    Title.Size = UDim2.new(1, 0, 0, 40)
    Title.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.Font = Enum.Font.SourceSans
    Title.TextSize = 20

    return Frame
end

-- Funkcia na vytvorenie tlačidla
function LumosUI:CreateButton(parent, text, callback)
    local Button = Instance.new("TextButton")

    Button.Name = "LumosUIButton"
    Button.Parent = parent
    Button.Text = text or "Click Me"
    Button.Size = UDim2.new(0, 150, 0, 40)
    Button.Position = UDim2.new(0.5, -75, 0.5, 0)
    Button.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.Font = Enum.Font.SourceSans
    Button.TextSize = 18
    Button.BorderSizePixel = 0

    Button.MouseButton1Click:Connect(function()
        if callback then
            callback()
        end
    end)

    return Button
end

-- Funkcia na vytvorenie posuvníka
function LumosUI:CreateSlider(parent, min, max, callback)
    local SliderFrame = Instance.new("Frame")
    local SliderBar = Instance.new("Frame")
    local SliderButton = Instance.new("TextButton")

    SliderFrame.Name = "SliderFrame"
    SliderFrame.Parent = parent
    SliderFrame.Size = UDim2.new(0, 300, 0, 30)
    SliderFrame.Position = UDim2.new(0.5, -150, 0.6, 0)
    SliderFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    SliderFrame.BorderSizePixel = 0

    SliderBar.Name = "SliderBar"
    SliderBar.Parent = SliderFrame
    SliderBar.Size = UDim2.new(1, 0, 0.5, 0)
    SliderBar.Position = UDim2.new(0, 0, 0.25, 0)
    SliderBar.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    SliderBar.BorderSizePixel = 0

    SliderButton.Name = "SliderButton"
    SliderButton.Parent = SliderBar
    SliderButton.Size = UDim2.new(0, 20, 1, 0)
    SliderButton.Position = UDim2.new(0, 0, 0, 0)
    SliderButton.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
    SliderButton.Text = ""
    SliderButton.BorderSizePixel = 0

    local dragging = false

    SliderButton.MouseButton1Down:Connect(function()
        dragging = true
    end)

    game:GetService("UserInputService").InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    game:GetService("RunService").RenderStepped:Connect(function()
        if dragging then
            local mousePos = game:GetService("UserInputService"):GetMouseLocation().X
            local framePos = SliderBar.AbsolutePosition.X
            local frameSize = SliderBar.AbsoluteSize.X
            local newPosition = math.clamp((mousePos - framePos) / frameSize, 0, 1)

            SliderButton.Position = UDim2.new(newPosition, 0, 0, 0)
            local value = math.floor((min + (max - min) * newPosition) + 0.5)

            if callback then
                callback(value)
            end
        end
    end)

    return SliderFrame
end

return LumosUI
